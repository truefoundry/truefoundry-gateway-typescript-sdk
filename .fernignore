# Paths Fern's TypeScript generator must leave alone on every regen. The
# hand-maintained top-level src/index.ts lives at a stable path so users can
# import across releases without Fern overwriting it.
src/index.ts
.github/workflows/ci.yml
